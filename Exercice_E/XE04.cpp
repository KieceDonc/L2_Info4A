/*
Réalisez en C (uniquement) une fonction
concat qui accepte en paramètres deux 
pointeurs sur char désignant des chaînes de
caractères (au format C) et retournant 
l'adresse d'une nouvelle chaîne, stockée dans
un tableau dynamique, qui contient la 
concaténation des chaînes désignées par les 
deux pointeurs passés en paramètres.
*/